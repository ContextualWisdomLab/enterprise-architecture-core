# Product / Technical Gap Baseline

This is the executable correction ledger for the Enterprise Architecture Decision Plane. It distinguishes shipped/default-branch truth, unmerged stack evidence, accepted architecture and external governance. A documented gap stays open until source/tests/settings prove the correction; wording alone does not close it.

## State model

- **Shipped/default-branch truth**: behavior present on the current repository default branch.
- **Active PR**: implemented only on an unmerged exact head; not shipped.
- **Accepted architecture**: responsibility/invariant that new work must obey.
- **Open DDD debt**: verified ownership/path/module debt requiring a mapped structural correction.
- **External governance**: an administrative/trusted-tool boundary owned outside this repository; this repository fails closed until it changes.
- **`planned_ui`**: an accepted presentation surface with no executable current-head UI evidence. Documentation diagrams or inventory prose do not make it shipped.

## Current baseline

| Area | State | Evidence / decision | Required next state |
| --- | --- | --- | --- |
| EA Decision Plane boundary | Accepted architecture | `docs/CONTEXT_MAP.md` and ADR 0001 keep capabilities, applications, interfaces, technologies, typed relations, lifecycle, portfolio assessment, strategy/transformation and scenarios in this repository. | Preserve EA authority while integrating foreign evidence through explicit contracts/Anti-Corruption Layers. |
| Technology Change Impact & Target-State Planner | Active PR stack | Migrations, runtime ports and tests extend the causal chain from technology lifecycle through impact, target-state planning, approval, execution, verification/recheck/replan and history. | Integrate dependency-first only after shared contract and governance gates; revalidate exact-current-head PostgreSQL/RLS/event/API behavior after every restack. |
| Cross-domain Data/AI evidence | Accepted supporting boundary | `data_management_*` tables/commands are receipt-bound EA decision/remediation projections. `semantic-data-portal` remains the system of record for catalog assets, glossary, lineage, domains, data products, output ports, contracts and trust/certification. | Keep source owner/truth/provenance explicit; never add direct portal-table SQL or silently promote inferred/proposed evidence. |
| Context Fabric consumer projection catalog | **Active PR correction** | `contracts/connectors/ecosystem.json` records explicit Context Fabric owner/direction boundaries. Context-bound projections use fully qualified repository owners, bind the single `contracts/context-graph-dependency.json` release manifest, and machine-declare preservation of canonical/source reference, truth status, effective/system time and provenance. `validation_connector_catalog.py` and focused regressions reject unbound projections, semantic loss and ambiguous repository owner references. | Keep every foreign product authoritative in its own repository. The dependency manifest must become an immutable released Context Graph package before integration; consumer-specific source protocols stay behind an ACL rather than becoming duplicated EA truth. |
| Quarantine Sandbox Runtime integration | **Active PR correction / external release dependency** | PR #40 now machine-declares `ContextualWisdomLab/quarantine-sandbox-runtime` as an independently deployable reusable runtime that owns isolation lifecycle/resource enforcement/cleanup/attestation and artifact-analysis evidence. Focused tests require both `contextual-orchestrator -> application_service_lease` and `Wardnet -> artifact_analysis_evidence`, prohibit direct database/source copy, and forbid malware verdict/artifact risk score from becoming authoritative EA facts. Fresh runtime producer evidence shows PR #1 and stacked caller-scoped lease PR #6 are still Draft and not released. | Keep the EA connector Draft/provisional until Quarantine Sandbox Runtime publishes a compatible immutable package/API and Context Graph publishes the required Context Assertion/CloudEvent release with conformance/admission evidence. Then bind exact released identities and revalidate the directional ACL interactions. |
| Executable EA visualization | **`planned_ui`** | No current tree in this stack contains an executable frontend, Figma-bound implementation or Storybook runtime. `docs/STORYBOOK_INVENTORY.md` classifies Capability/Application maps, risk/impact graphs, scenario/current-target comparison, transformation timeline and evidence drawer as `planned_ui`. | Activate the UI delivery gate only with the first executable presentation slice: Figma/Product Design + Storybook + current-head browser interaction, WCAG 2.2 AA exact-value alternatives, responsive screenshots, semantic/current-target/time/provenance correctness, large-data performance and lifecycle cleanup. |
| `src/ea_core_foundation` package name | **Open DDD debt** | The foundation-era name no longer describes the bounded contexts now implemented. Renaming the entire public package blindly would touch imports, package metadata, tests and consumers and could create compatibility breakage. | Build an exact consumer/import/package/API/event map after `main` governance stabilizes; introduce bounded-context-oriented package paths through a versioned compatibility window if public imports require it; remove the obsolete alias only after consumers migrate. |
| Historical `src/ea_core_foundation/service.py` path | **Active PR correction + remaining DDD debt** | The ~42 KiB implementation has been moved unchanged to the responsibility-named `decision_plane_http.py`; `service.py` is now a behavior-free compatibility facade and an architecture-fitness regression prevents domain/application behavior from returning to the generic path. Existing internal and possible external imports still use the historical facade, and `decision_plane_http.py` still spans more than one bounded responsibility. | Migrate internal consumers from the compatibility path, map external imports, then split coherent HTTP/application slices by bounded context. Remove the facade only after the compatibility window is proven safe. |
| Broad runtime/validation modules | **Active correction + open DDD debt** | `validation_connector_catalog.py` now owns new ecosystem projection validation instead of adding that behavior to the broad historical `validation_core.py` chain. `runtime.py`, `validation.py`, `validation_core.py` and other feature-specific runtime/validation modules still reflect foundation-first technical layering. | Continue moving only mapped coherent responsibilities into bounded-context modules. Preserve compatibility where a real import contract exists; do not turn the new module into another generic validation bucket. |
| Canonical Context Map and Ubiquitous Language | Active PR correction | These canonical DDD artifacts were absent from the top functional stack. `tests/test_ddd_architecture_fitness.py` now requires them and this baseline. | Keep the executable requirement after stack/base migration and update terms when code ownership actually changes. |
| Generic bucket / foreign implementation creep | Active PR prevention | Architecture fitness rejects new catch-all directories/modules and direct imports from known foreign CWL product implementations. `cwl_context_contracts` remains an allowed upstream Shared Kernel dependency. | Extend only for observed dependency classes; do not replace semantic boundary tests with broad textual bans. |
| PostgreSQL authority / tenant boundary | Accepted architecture + active evidence | Database migrations/tests use tenant-safe references, RLS/runtime-role checks, bitemporal history and transactional event patterns. Caller-set tenant context is filtering context, not authorization by itself. | Continue executable clean-install/upgrade/checksum/RLS/cross-tenant/outbox-inbox verification on every integrated exact head. |
| PostgreSQL terminal-planner acceptance fixture isolation | **Active PR correction / RCA** | PR #40 exact predecessor `82d099d5a728efc8bf0bc846e5207b3ee6a1673b` failed only the PostgreSQL terminal-planner acceptance after migrations, rollback rehearsal and earlier invariants passed. The suite retained legacy transformation `0196e010-1111-7111-8111-111111111111` and terminal-state transformation `0196e010-1111-7111-8111-111111111191` under the same scenario/remediation path; the planner's deterministic UUID ordering selected the older transformation, so the terminal fixture observed `started` rather than the verified state it was designed to exercise. Commit `4efdc17d751a1fcb596e1e40d4dd2d645a134df5` isolates the acceptance fixture by temporarily superseding only the competing legacy transformation inside the test transaction; the final rollback restores it for the later historical planner coverage. No production planner rule, security/review gate, or migration semantics were weakened. | Require a fresh exact-head `postgres-migration` success after the fixture isolation and after this RCA documentation commit. If selection among multiple active transformations becomes a product requirement, specify and test that policy separately instead of encoding it through test UUID order. |
| Solo-maintainer counted-review rule | External governance / open | Live organization ruleset `18156473` requires one approving review but names no required reviewer/team. Current Context Fabric governance forbids self-approval and bot/model-as-human while operating without a separate human reviewer authority. Central owner path `ContextualWisdomLab/.github#772` carries the scoped solo-compatible repair acceptance. | Remove only the structurally unsatisfiable approving-review count for these repositories while preserving deterministic required workflows, security/coverage/package/SBOM/provenance evidence, thread resolution, deletion/non-fast-forward protection and no routine bypass. Re-read the live rule before integration. |
| Central coverage/OpenCode trusted toolchain | External governance / open | Root #21 repository-owned CI/runtime/supply-chain are green, but central coverage evidence fails because the trusted dispatcher runs `uv 0.12.1` while this repository requires `uv ==0.11.32`. Exact reproduction was advanced to `ContextualWisdomLab/.github#1234`. | Central trusted tooling must honor the target repository's exact `tool.uv` requirement without allowing PR-controlled execution or weakening offline trust; then produce fresh substantive exact-head review evidence. |
| `main` integration/default governance | External governance / open | Program architecture requires `main` as intended integration/default. Repository metadata still reports `develop`; `develop@1c0fa8b15ceb9e72186274aeb255d6777eb84ef4` is protected while `main@ca6889497728e1a3f09d68790a9096576e13a3ff` is unprotected. Root #21 already targets `main`. Central owner path `.github#1137` was updated with the superseding acceptance. | Protect `main` with effective checks/review-policy/thread/deletion/non-fast-forward/no-routine-bypass controls, make repository metadata coherent, then rebuild every PR base/ancestry against the fresh live branch. Do not transfer predecessor evidence. |

## Structural correction sequence

The historical package is not renamed as cosmetic churn. Before moving a bounded slice, enumerate public imports, internal dependencies, OpenAPI/AsyncAPI/schema paths, database migrations, tests, workflow/package metadata and external consumers. Add a regression for the intended dependency direction, move one coherent responsibility, provide a versioned adapter only when an actual consumer requires it, and remove the old path after the deprecation window. A modular monolith remains the default while these contexts share transaction/deployment cohesion.

The first bounded correction has moved the broad process/HTTP implementation out of the generic `service.py` path without changing its behavior or breaking its import surface. That compatibility step is intentionally incomplete: new behavior must target `decision_plane_http.py` or a more specific bounded-context path, while internal consumers and the broad implementation are decomposed in subsequent mapped slices.

The connector-catalog correction follows the same rule: the existing historical validation chain remains a compatibility dependency, while new Context Fabric ownership/release-binding invariants live in `validation_connector_catalog.py`. This is a bounded step, not permission to grow a second catch-all validation module.

The Quarantine Sandbox Runtime slice deliberately records only a connector/ACL boundary. It does not copy Rust runtime types, Podman/container mechanics, malware verdicts, artifact risk scores, runtime storage or caller policy into EA. The connector can advance from provisional to integrable only when both the runtime and Context Graph dependency have immutable released identities that the EA release gate can verify.

The architecture-fitness test blocks new generic buckets and direct foreign implementation imports now, so the correction ledger cannot be used as permission to make the existing shape worse while a larger migration waits.

## Release statement

No active-stack capability, DDD correction, connector projection or buyer-facing planner flow is release truth until it is integrated on the intended protected `main` exact head and all applicable repository CI/security/coverage/migration/RLS/outbox-inbox/package/SBOM/provenance/reproducibility/review-policy/operational gates pass together. Every context-bound connector additionally depends on the immutable released `context-graph-contracts` identity recorded by `contracts/context-graph-dependency.json`. Quarantine integration additionally requires an immutable released Quarantine Sandbox Runtime consumer contract; neither its current Draft PRs nor CGC PR #21 are release evidence. This baseline is not evidence of certification, adoption, UI delivery or acquisition valuation.
